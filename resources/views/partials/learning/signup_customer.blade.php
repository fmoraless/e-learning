<form class="intro-newslatter text-center">
    <div class="row justify-content-center">
        <div class="col-12 mb-3">
            <input type="text" placeholder="{{ __("Nombre") }}">
            <input type="text" class="last-s" placeholder="{{ __("Correo electrónico") }}">
        </div>
        <div class="col-12 no-gutters">
            <input type="password" placeholder="{{ __("Contraseña") }}">
            <input type="password" class="last-s" placeholder="{{ __("Confirma la contraseña") }}">
        </div>
        <div class="col-lg-12 mt-3">
            <button class="site-btn btn-block">{{ __("Crear cuenta") }}</button>
        </div>
    </div>
</form>
